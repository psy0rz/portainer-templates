FROM node:21

# RUN npm install --global gulp gulp-sass node-sass gulp-concat
# RUN npm install --global sass

# RUN apt update && apt install python3 -y


WORKDIR /repo


CMD npm install && npx gulp 

